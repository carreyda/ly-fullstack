# 安全策略

LY Fullstack 是面向个人、小团队和中小型项目的全栈管理底座。安全问题需要被认真处理，但项目不会把尚未实现的企业级安全基础设施描述为现有能力。

## 支持范围

| 版本     | 安全更新 |
| -------- | -------- |
| 0.1.x    | 支持     |
| 更早版本 | 不支持   |

项目仍处于早期阶段。安全修复会优先进入最新的 `0.1.x` 版本，不承诺为历史提交长期维护补丁分支。

## 私密报告安全问题

登录绕过、权限提升、JWT 或密钥泄露、SQL 注入、任意代码执行、敏感数据暴露以及可造成数据破坏的问题，请不要提交公开 Issue。

请通过 GitHub 的 [Private Security Advisory](https://github.com/liangy0323/ly-fullstack/security/advisories/new) 私密报告，并尽量提供：

- 受影响的版本、Commit 或部署方式。
- 可重复的最小复现步骤。
- 实际影响与攻击前提。
- 必要的请求、响应或日志，且必须移除真实密码、Token、数据库连接串和个人数据。
- 已知的缓解方式或修复建议。

维护者会尽快确认报告是否可复现，并在确认影响后协调修复与披露时间。项目暂不承诺固定响应时限或漏洞奖励，不应把未建立的 SLA 写成对外保证。

## 当前安全基线

- 管理端密码使用 bcrypt 哈希，数据库和接口响应不返回密码哈希。
- JWT 包含账号会话版本；修改密码或后台重置密码会撤销此前签发的全部 Token。
- Guard 每次受保护请求都会重新读取账号、角色和菜单状态，账号禁用与权限收回不依赖旧 Token 快照。
- 登录接口按“来源 IP + 账号”执行每分钟 5 次的服务端限流，触发后阻断 1 分钟。
- Fastify Helmet 为 API 响应添加常见浏览器安全头。
- `pnpm setup` 要求用户自行设置管理员初始密码，不提供项目通用的默认管理员密码。
- 服务端秘密只允许通过私有环境文件、CI Secret 或部署平台注入，不得进入浏览器产物和 Git。

## 已知边界

当前登录限流使用单个 Node.js 进程的内存计数器，适合仓库默认的单实例模块化单体部署。多实例或多地域部署必须使用网关、WAF、云平台限流或共享存储，否则不同实例之间不会共享计数。

登录页滑块只改善普通交互，不能阻止绕过浏览器直接调用 API。公开互联网部署建议叠加阿里云或其他云平台的成熟人机验证、WAF 与异常登录监控。

当前使用单 Access Token 与会话版本撤销机制，没有实现 Refresh Token 轮换、设备会话管理和 Redis Token 黑名单。该选择符合当前项目规模边界；需要更强会话治理时应按真实威胁模型扩展，不能只为了技术名词堆叠基础设施。

官方部署方案默认由 Nginx 提供 HTTPS 入口，Admin API 不应直接暴露公网。完整上线要求见 [`docs/deployment.md`](docs/deployment.md)。

## 部署者责任

- 使用独立高强度的数据库密码、管理员密码和至少 32 字节随机 JWT 密钥。
- 全站启用 HTTPS，并限制 PostgreSQL 与 Admin API 的网络访问来源。
- 定期备份并实际演练恢复，升级前执行 migration 检查。
- 根据公网暴露程度接入云平台人机验证、WAF、日志告警和共享限流。
- 及时升级依赖和 LY Fullstack 安全版本，不把 Demo 或测试凭证用于生产。
- 不在 Issue、截图、日志、聊天记录或构建产物中公开真实秘密。

## 普通问题

普通功能缺陷、兼容问题和改进建议请使用公开 Issue 模板。无法确定是否属于安全问题时，优先使用私密 Security Advisory。

LY Fullstack 项目组
