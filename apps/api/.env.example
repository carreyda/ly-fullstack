# 默认 C 端 API 环境变量契约。pnpm setup 只生成本地 .env.development。
# test 与 production 由 CI/CD 或部署平台注入。

DATABASE_URL=""

# 按真实 C 端地址配置浏览器跨域白名单，多个来源使用英文逗号分隔。
CORS_ORIGINS="http://localhost:3001,http://127.0.0.1:3001"

# 服务端口由 workspace 配置或部署平台通过 PORT 注入。
PORT=""
