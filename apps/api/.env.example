# 复制为 `.env.development`、`.env.test` 或 `.env.production` 并按环境填写；运行环境文件不入库。

# 允许 C 端主站跨域请求 API 的来源，多个地址使用英文逗号分隔。
# 主站尚未创建，未配置时服务保持同源访问，不开放跨域来源。
CORS_ORIGINS=""

# C 端 API 服务监听端口，未提供时使用默认端口 3000
PORT=3000
