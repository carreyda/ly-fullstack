# 复制为 `.env.development`、`.env.test` 或 `.env.production` 并按环境填写。
# APP_ENV 必须与 Rsbuild 的 --env-mode 一致，构建时校验。

# 当前运行环境
APP_ENV=development

# 管理 API 的基础地址；开发环境直接请求 NestJS，生产环境通常使用同源 `/api`
API_BASE_URL=http://127.0.0.1:3001/api
