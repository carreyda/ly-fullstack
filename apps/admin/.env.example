# 管理后台环境变量模板。development、test、production 都是公开的浏览器构建配置，直接提交仓库。
# pnpm setup 会校验 .env.development 中的 Admin API 端口是否与 workspace.config.json 一致。

# 当前运行环境
APP_ENV=development

# 管理 API 的基础地址；开发环境直接请求 NestJS，生产环境通常使用同源 `/api`
API_BASE_URL=http://127.0.0.1:3000/api
