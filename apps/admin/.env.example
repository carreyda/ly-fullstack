# 管理后台环境变量契约。执行 pnpm setup 会据此生成三套被 Git 忽略的运行文件。
# 手动维护时，APP_ENV 必须与 Rsbuild 的 --env-mode 一致，构建阶段会执行校验。

# 当前运行环境
APP_ENV=development

# 管理 API 的基础地址；开发环境直接请求 NestJS，生产环境通常使用同源 `/api`
API_BASE_URL=http://127.0.0.1:3000/api
