/**
 * `base-icon` 允许使用的图标名
 *
 * 只登记真实用到的图标，保持联合类型与 `BASE_ICON_COMPONENT_MAP` 一一对应。
 */
export type BaseIconName =
  'ArrowRight' | 'CaretBottom' | 'DataBoard' | 'Fold' | 'FullScreen' | 'Link' | 'Menu' | 'Monitor' | 'Refresh' | 'User';
