/**
 * 健康检查响应中的服务标识
 *
 * 该值只用于帮助监控系统区分服务实例，不参与路由注册，也不会成为 URL 前缀。
 */
export const HEALTH_SERVICE_NAME: string = '{{name}}';

/**
 * 浏览器跨域请求允许使用的 HTTP 方法
 */
export const SERVER_CORS_METHODS: string[] = ['GET', 'HEAD', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'];
