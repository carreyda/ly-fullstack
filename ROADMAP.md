# Roadmap

LY Fullstack 的路线图按真实能力演进，不以堆叠依赖或目录数量作为完成标准。规划没有固定发布日期，只有实现、测试、文档和部署边界全部闭环后才进入公开版本。

## 已完成：v0.1.0

- 通用 Admin 与 Admin API 管理核心。
- JWT、动态菜单和五表 RBAC。
- 用户、角色、菜单管理闭环。
- PostgreSQL migration、Seed 与交互式 Setup。
- 深浅主题、统一设计系统和 Element Plus 定制方案。
- Rstest、Playwright 冒烟测试和 GitHub Actions 质量门禁。
- 版本检测、离线缓存与生产部署文档。
- MIT、Release Notes 和贡献流程。

## 稳定化：v0.1.x

- 根据真实使用反馈修复初始化、权限和跨平台问题。
- 扩展 Admin 与 Admin API 的关键单元测试、集成测试和浏览器测试。
- 在具备服务器资源后发布公开只读 Demo。
- 对官方 Linux + Nginx + Systemd 部署方案进行真实服务器演练。
- 完善安全报告、备份恢复和发布检查流程。

## 场景方案：v0.2.x

- 选择一个明确的 C 端场景，提供独立业务 API 与客户端参考实现。
- 客户端技术栈由真实场景决定，可以是 Nuxt、Next.js、小程序或其他方案。
- 继续保持 Admin 管理核心与具体业务解耦，不把示例场景固化为核心仓库默认能力。
- 补充从业务 API、客户端到管理后台的完整测试和部署说明。

## 独立规划：微服务版本

- 创建独立的 NestJS 微服务解决方案，不把当前仓库强行改造成微服务。
- 覆盖 API 网关、服务通信、消息可靠性、认证传播和服务间契约。
- 覆盖链路追踪、集中日志、健康检查、容器化部署和分布式测试。
- 使用真实跨服务业务链路验证架构，不发布只有空目录的“微服务模板”。

## 非目标

- 不为展示技术复杂度而提前拆分微服务。
- 不在核心仓库预置无法确定的 C 端空应用。
- 不承诺一套默认架构覆盖所有并发量、组织规模和合规场景。
- 不把未部署、未测试或只有文档描述的能力列为已完成。

欢迎通过 Issue 讨论真实使用场景。功能进入 Roadmap 不代表已经承诺发布日期，最终范围以维护者确认的版本计划为准。

LY Fullstack 项目组
